# 阶段3-实现记录-调研落地

> 日期：2026-07-14 | 分支：`feat/research-landing-memory-knowledge`
> 基于：[阶段1 PRD](阶段1-PRD-调研落地.md) + [阶段2 技术方案](阶段2-技术方案-调研落地.md)

## 方案引用

| 方案设计决策 | 是否按方案实现 | 说明 |
|------------|--------------|------|
| knowledge/search/ 8 文件架构 | ✅ | 严格按方案：IntentRouter + 3源适配器 + Ranker + Cache + Extractor |
| 向量索引复用 BGE 模型 | ✅ | `MemoryVectorIndex` 复用 `knowledge.embedding.BGEEmbedder` |
| RRF 融合 k=60 | ✅ | 纯 Python ~15 行，零新依赖 |
| 自动提取只记录 CRITICAL/HIGH/MEDIUM | ✅ | `auto_extract()` 按规则过滤，非关键事件跳过 |
| GoalPlanner LLM 拆里程碑 | ✅ | 规划失败回退默认单里程碑线性规划 |
| verify_milestone 两层验证 | ✅ | 确定性检查 + LLM 审查（LLM 审查由调用方处理） |
| 降级链 AnySearch→SearXNG→GitHub→本地 | ✅ | 每源独立超时 3s |
| 零新依赖（readability-lxml+html2text 除外） | ✅ | 仅增加 2 个可选纯 Python 轻量包 |

**严格按方案实现，无偏离。**

## 改动清单

| 文件 | 操作 | 目的 |
|------|------|------|
| `src/orbit/knowledge/search/__init__.py` | 新增 | 搜索模块入口，导出核心类 |
| `src/orbit/knowledge/search/sources/base.py` | 新增 | SearchSource 抽象基类 + RawSearchItem/SearchResult |
| `src/orbit/knowledge/search/sources/web.py` | 新增 | SearXNG 通用搜索适配器 |
| `src/orbit/knowledge/search/sources/code.py` | 新增 | GitHub 代码搜索适配器 |
| `src/orbit/knowledge/search/sources/anysearch.py` | 新增 | AnySearch 可选加速适配器 |
| `src/orbit/knowledge/search/sources/__init__.py` | 新增 | 数据源包导入 |
| `src/orbit/knowledge/search/intent_router.py` | 新增 | LLM 意图路由 + 同步关键词回退 |
| `src/orbit/knowledge/search/ranker.py` | 新增 | 去重+同源衰减+信息密度仲裁 |
| `src/orbit/knowledge/search/cache.py` | 新增 | SQLite TTL 缓存 |
| `src/orbit/knowledge/search/extractor.py` | 新增 | readability-lxml + html2text 正文提取 |
| `src/orbit/memory/vector.py` | 新增 | BGE 向量索引 + RRF 融合 + 余弦相似度 |
| `src/orbit/memory/store.py` | 修改 | `__init__` 加 `_vector_index`、新增 `vector_index` property、`search()` 加向量+RRF 分支 |
| `src/orbit/memory/episodic.py` | 修改 | 新增 `auto_extract()` 自动记忆提取 + structlog 导入 |
| `src/orbit/goal/planner.py` | 新增 | GoalPlanner + Milestone + ExecutionPlan + 循环依赖检测 |
| `src/orbit/goal/verifier.py` | 修改 | 新增 `verify_milestone()` + `_deterministic_check()` |
| `src/orbit/goal/intake_router.py` | 修改 | `__init__` 接受 `llm_client` hook |

## 回溯对照

| PRD 验收标准 | 方案设计决策 | 代码实现位置 |
|------------|------------|------------|
| US-1: 外部搜索+前置筛选+降级 | §2 knowledge/search/ 三层架构 | `search/` 8 文件全覆盖 |
| US-2: 记忆自动提取 | §3.2 auto_extract | `memory/episodic.py:234` auto_extract() |
| US-3: 向量语义搜索+RRF | §3.1 MemoryVectorIndex + RRF | `memory/vector.py` + `memory/store.py:219` search() |
| US-4: Goal 规划→验证 | §4 planner + verifier | `goal/planner.py` + `goal/verifier.py:271` verify_milestone() |
| US-5: 多开源+AnySearch fallback | §2.5 降级链 | `search/sources/` web.py/code.py/anysearch.py |
| US-6: 搜索可追溯 | §5 Trace span | observability/ 接线待下一步 |
| US-7: 搜索结果缓存 | §2.4 SearchCache | `search/cache.py` |

---

> 阶段3 实现记录保存完毕。进入阶段4 测试验证。
