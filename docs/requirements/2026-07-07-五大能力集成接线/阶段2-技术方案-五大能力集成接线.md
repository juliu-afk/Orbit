# 阶段2 技术方案 — 五大能力模块集成接线（v3·框架级）

> v1→v2：闭环验证发现 6 断点。v2→v3：站在 Orbit 框架整体看依赖注入和生命周期。

## 1. 需求回顾

基于阶段1 PRD，将 16 个已实现模块中未接入/半接入的 11 项接入执行循环。验收标准 AC1-AC9。

## 2. 框架级架构分析

### 2.1 当前组装图（main.py）

```
main.py（模块级组装——所有组件在此初始化）
├─ _event_bus         ← 唯一 EventBus 实例
├─ _agent_llms        ← LLMClient dict（6 个角色×3 Tier）
├─ _compressor        ← ContextCompressor
├─ _budget_tracker    ← TokenBudgetTracker
├─ _code_graph_engine ← CodeGraphEngine（SQLAlchemy async）
├─ _checkpoint_manager← CheckpointManager（Redis/内存）
├─ _scheduler         ← Scheduler → TaskRunner → AgentFactory → ReActAgent
├─ _meta_orchestrator ← MetaOrchestrator（goal/）
├─ _compose_orchestrator← ComposeOrchestrator（compose/）
├─ _dream_engine      ← DreamEngine
├─ _actor_*           ← Actor 子系统
└─ lifespan:
     ├─ TrajectoryCollector("data/trajectories.db")  ← 第 422 行：与 wiring 重复！
     └─ ...
```

### 2.2 问题：Wiring 游离于框架之外

```
当前:
  get_wiring() → OrbitWiring()  ← 模块级懒初始化，无参数
    ├─ db_path=":memory:"       ← 无持久化
    ├─ 无 event_bus             ← HITL 断链
    └─ 与 main.py TrajectoryCollector 重复

目标:
  main.py 初始化 OrbitWiring → 注入 event_bus + db_path → 作为全局单例
    ├─ TrajectoryCollector      ← 统一（删 lifespan 重复）
    ├─ EpisodicMemory           ← 持久化
    ├─ ProfileStore             ← 持久化
    ├─ AgenticMemory            ← 持久化
    ├─ DistillationEngine       ← 持久化
    ├─ AnchorGuard              ← 持久化
    ├─ HITLManager(event_bus)   ← HITL 闭环
    └─ MonitorAgent(hitl_manager)← Monitor→HITL 闭环
```

### 2.3 执行链路（端到端）

```
用户 → chat WebSocket → ChatterAgent/ClarifierAgent → compose → scheduler
                                                            │
task_runner.run_task(task_id, prd)                          │
├─ wiring.on_task_start()          ← 轨迹开始                │
├─ wiring.load_profile()           ← 用户画像                │
├─ wiring.start_monitor()          ← Monitor + HITLManager   │
│   └─ MonitorAgent(hitl_manager)  ← HITL 已接线             │
│                                                                  │
└─ _agent_cycle() → _run_agent()                                   │
     ├─ ReflectionEngine(llm)      ← NEW: 创建                   │
     ├─ PreActEngine(llm, tools)   ← NEW: 创建                   │
     ├─ VigilSelfHealer()          ← NEW: 创建                   │
     └─ factory.create(..., ref_engine, preact, vigil)           │
          │                                                       │
          └─ ReActAgent.execute_stream()                          │
               ├─ PreAct: predict → skip risky actions            │
               ├─ Tool dispatch → TOOL_RESULT                     │
               │   ├─ feed_monitor(event)  ← Monitor 收到事件     │
               │   └─ record_event(...)    ← 情节记忆记录         │
               ├─ On error: vigil.diagnose() → heal()             │
               │   └─ inject user message                         │
               ├─ ReflAct: reflect → drift? → inject correction    │
               └─ FINISH: record_event(...)                       │
                                                                  │
wiring.maybe_distill()          ← 每 10 个任务                    │
├─ anchor.check_before_distill  ← ANCHOR 蒸馏前检查               │
├─ de.distill_from_trajectory   ← 规则蒸馏                        │
└─ anchor.check_after_distill   ← ANCHOR 蒸馏后检查（llm_distill） │
```

## 3. 改动清单（7 文件，+~130 行）

| # | 文件 | 类型 | 规模 | 内容 |
|---|------|------|------|------|
| 1 | **main.py** | 修改 | +15 行 | 初始化 OrbitWiring、注入 event_bus+db_path、删重复 TrajectoryCollector |
| 2 | **integration/wiring.py** | 修改 | +40 行 | configure()、feed_monitor()、load_profile()、HITLManager 创建、ANCHOR pre-distill |
| 3 | **task_runner/runner.py** | 修改 | +20 行 | 统一 get_wiring()、创建 Engine 三件套、load_profile |
| 4 | **task_runner/checkpoint.py** | 修改 | -15 行 | 删 `_get_wiring()` 和 `_wire()` |
| 5 | **agents/factory.py** | 修改 | +8 行 | 接受 preact_engine + vigil_healer + 传递 |
| 6 | **agents/react_agent/agent.py** | 修改 | +25 行 | feed_monitor、record_event、VIGIL 诊断注入、vigil_healer 构造参数 |
| 7 | **api/routes/sessions.py** | 不改 | 0 | Profile 加载移到 task_runner |

## 4. 逐文件详细设计

### 4.1 main.py（框架组装层）

**位置**：模块级初始化区域（`_scheduler = Scheduler(...)` 之后）

```python
# ── OrbitWiring: 五大能力集成接线（Phase F）──
from orbit.integration.wiring import configure_wiring

_wiring = configure_wiring(
    db_path=str(settings._BASE / "data" / "orbit_wiring.db"),
    event_bus=_event_bus,
)
```

**lifespan 修改**：删除第 422-425 行的独立 TrajectoryCollector——wiring 内部已管理。

```python
# 删除这 4 行：
# from orbit.observability.trajectory import TrajectoryCollector
# _trajectory_collector = TrajectoryCollector(db_path="data/trajectories.db")
# app.state.trajectory_collector = _trajectory_collector
# logger.info("trajectory_collector_initialized", ...)

# 改为：
app.state.trajectory_collector = _wiring._get_trajectory()
logger.info("orbit_wiring_initialized", db_path=str(settings._BASE / "data" / "orbit_wiring.db"))
```

### 4.2 integration/wiring.py（接线核心）

**新增 `configure_wiring()` 函数**——框架级初始化入口：

```python
def configure_wiring(db_path: str, event_bus: EventBus | None = None) -> OrbitWiring:
    """框架级初始化——main.py 调用，注入依赖。
    
    替代模块级懒初始化 get_wiring()——后者仍可用作回退。
    """
    global _wiring_instance
    _wiring_instance = OrbitWiring(db_path=db_path)
    _wiring_instance._event_bus = event_bus
    return _wiring_instance
```

**OrbitWiring 新增方法**：

```python
def feed_monitor(self, task_id: str, event: dict) -> None:
    """向 Monitor 队列推送事件——react_agent 在 TOOL_RESULT 后调用。"""
    q = self._monitor_queues.get(task_id)
    if q and q.qsize() < 100:  # maxsize 保护
        try:
            q.put_nowait(event)
        except asyncio.QueueFull:
            pass

def load_profile(self, project_id: str) -> UserProfile | None:
    """加载用户画像——task_runner 在任务开始时调用。"""
    ps = self._get_profile()
    if ps:
        return ps.get_profile(project_id)
    return None
```

**start_monitor() 修订**——创建 HITLManager + 传入 MonitorAgent：

```python
async def start_monitor(self, task_id: str, goal: str = "") -> asyncio.Task | None:
    mon = self._get_monitor()
    if mon is None:
        return None
    
    # 创建 HITLManager（如果有 event_bus）
    hitl = None
    if self._event_bus:
        hitl = HITLManager(event_bus=self._event_bus)
    
    mon._goal = goal
    mon._task_id = task_id
    mon._hitl = hitl  # 注入 HITLManager
    
    queue: asyncio.Queue = asyncio.Queue(maxsize=100)
    self._monitor_queues[task_id] = queue
    task = asyncio.create_task(mon.run(queue, task_id=task_id))
    return task
```

**maybe_distill() 修订**——加 ANCHOR pre-distill：

```python
async def maybe_distill(self) -> None:
    if self._task_count % DISTILL_EVERY_N_TASKS != 0:
        return
    # ... existing code ...
    for t in completed[:10]:
        exported = tc.export_for_training(t["trajectory_id"])
        # ANCHOR: 蒸馏前检查
        anchor = self._get_anchor()
        if anchor:
            verdict = anchor.check_before_distill(exported)
            if verdict.verdict == SupervisionVerdict.REJECTED:
                continue
        de.distill_from_trajectory(exported)
```

### 4.3 task_runner/runner.py（创建 Engine 三件套）

**_run_agent() 修订**（L330 附近，agent_factory.create() 之前）：

```python
# Phase F: 创建五大能力引擎实例
ref_engine = ReflectionEngine(llm=llm) if llm else None
pre_engine = PreActEngine(llm=llm, tools=self._tool_registry) if llm and self._tool_registry else None
vigil = VigilSelfHealer()

agent = self._agent_factory.create(
    role, llm=llm, graph=self._graph, sandbox=None,
    tools=self._tool_registry, event_bus=self._event_bus,
    compressor=self._compressor, budget_tracker=self._budget_tracker,
    reflection_engine=ref_engine,     # Phase A
    preact_engine=pre_engine,         # Phase D
    vigil_healer=vigil,               # Phase D
)
```

**run_task() 修订**——加 Profile 加载：

```python
# v0.21 关键词提取后
context["keywords"] = keywords

# Phase F: 加载用户画像
try:
    wiring = get_wiring()
    project_id = context.get("project_id", "")
    if project_id:
        profile = wiring.load_profile(project_id)
        if profile:
            context["user_profile"] = profile
except Exception:
    pass  # fail-open
```

### 4.4 task_runner/checkpoint.py（删自建实例）

删除 `_wire()` 方法和 `_get_wiring()` 方法。  
`_wire("on_task_end", ...)` 调用改为直接 `get_wiring().on_task_end(...)`。  
`maybe_distill()` 调用改为 `get_wiring().maybe_distill()`。

### 4.5 agents/factory.py（传递新参数）

**create() / get_agent() 签名新增**：

```python
preact_engine: PreActEngine | None = None,   # Phase D: PreAct
vigil_healer: VigilSelfHealer | None = None,  # Phase D: VIGIL
```

**ReActAgent 构造传递**：

```python
agent = agent_cls(
    ...,
    reflection_engine=reflection_engine,
    preact_engine=preact_engine,     # NEW
    vigil_healer=vigil_healer,       # NEW
)
```

### 4.6 agents/react_agent/agent.py（消费引擎）

**构造参数新增**：

```python
vigil_healer: VigilSelfHealer | None = None,  # Phase D: VIGIL 自愈
```

**execute_stream() 新增三处调用**：

① TOOL_RESULT 后推送 Monitor（L410 附近）：

```python
yield StreamEvent(type=StreamEventType.TOOL_RESULT, ...)

# Phase F: 推送事件到 Monitor
try:
    wiring = get_wiring()
    wiring.feed_monitor(task_id, {
        "type": "tool_result",
        "data": {"tool": tool_name, "args": tool_args, "result_size": len(result_str)}
    })
except Exception:
    pass
```

② 工具错误时 VIGIL 诊断（L391-394 替换）：

```python
except Exception as e:
    error_text = str(e)
    if self._vigil_healer:
        try:
            diagnosis = self._vigil_healer.diagnose(error_text)
            if diagnosis.auto_fixable:
                heal = self._vigil_healer.heal(diagnosis, tool_name, tool_args)
                messages.append({"role": "user", "content": f"[VIGIL 自愈建议] {heal.message}"})
        except Exception:
            pass  # VIGIL fail-open
    result_str = f"工具执行失败: {error_text}"

# Phase F: 记录情节事件
try:
    wiring = get_wiring()
    wiring.record_event(task_id, f"Tool error: {tool_name}", "failure", [tool_name])
except Exception:
    pass
```

③ ReflAct 漂移时记录事件（L496 附近）：

```python
if reflection.is_drifting():
    logger.info("reflact_drift_detected", ...)
    # Phase F: 记录漂移事件
    try:
        get_wiring().record_event(task_id, "Goal drift detected", "drifted", ["drift"])
    except Exception:
        pass
```

## 5. 断点修复对照

| # | 断点 | 严重度 | 修复 | 位置 |
|---|------|--------|------|------|
| 1 | DB `:memory:` 全链路失忆 | 🔴 | `configure_wiring(db_path=.../"orbit_wiring.db")` | main.py |
| 2 | Monitor→HITL 断链 | 🔴 | `start_monitor()` 创建 `HITLManager(event_bus)` | wiring.py |
| 3 | 双实例 wiring | 🟡 | 删 `_get_wiring()`，统一 `get_wiring()` | checkpoint.py |
| 4 | Profile 加载时机 | 🟡 | `run_task()` 中 `wiring.load_profile(project_id)` | runner.py |
| 5 | MCTS 无触发 | 🟢 | P2 延后——接口已就位 | — |
| 6 | VIGIL 注入模糊 | 🟡 | `HealResult.message` → `[VIGIL 自愈建议]` user msg | agent.py |
| 7 | TrajectoryCollector 重复 | 🟡 | 删 lifespan 中独立实例，统一用 wiring | main.py |

## 6. 兼容性保证

| 场景 | 行为 |
|------|------|
| `configure_wiring()` 未调用 | `get_wiring()` 回退到 `OrbitWiring(":memory:")`——旧行为 |
| event_bus=None | HITLManager 不创建，Monitor 告警只记日志 |
| LLM=None（mock 模式） | ReflectionEngine/PreActEngine 设为 None，不执行 |
| wiring DB 目录不存在 | SQLite 自动创建 |
| 测试环境 | `configure_wiring(db_path=":memory:")` 隔离 |

## 7. PRD 对照表

| AC | 实现 | 位置 |
|----|------|------|
| AC1 ReflectionEngine | `_run_agent()` 创建 → factory 传递 | runner.py + factory.py |
| AC2 PreActEngine | 同上 | runner.py + factory.py |
| AC3 Monitor 事件+HITL | `feed_monitor()` + `start_monitor()` 创建 HITLManager | wiring.py + agent.py |
| AC4 情节记忆 | `record_event()` 在错误/漂移时调用 | agent.py |
| AC5 VIGIL | 工具错误→diagnose→heal→user message | agent.py |
| AC6 ANCHOR pre-distill | `maybe_distill()` 蒸馏前调 `check_before_distill()` | wiring.py |
| AC7 Profile | `run_task()` 中 `load_profile()` → context | wiring.py + runner.py |
| AC8 MCTS | `get_mcts_planner()` 接口就位（P2 延后） | wiring.py |
| AC9 测试 | 全 fail-open，现有测试全过 | CI |

## 8. 不在本次范围（明确 Non-Goals）

| 项目 | 理由 |
|------|------|
| MCTS 完整集成 | 需要评估函数+候选集，超出"接线"范围 |
| Profile 注入 PromptBuilder | 涉及 Prompt 模板改造，另开需求 |
| GRPO 强化学习 | 需要训练基础设施，Phase C 后续 |
| GEPA Prompt 进化 | 需要进化触发策略，Phase E 后续 |
| 前端 HITLModal 改造 | 已有组件，本次不修改 |
