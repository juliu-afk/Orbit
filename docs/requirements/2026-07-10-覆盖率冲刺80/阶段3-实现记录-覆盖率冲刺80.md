# 阶段3-实现记录-覆盖率冲刺80

> 基于阶段2 技术方案。本迭代完成第 1 批 + 部分第 2 批。

## 方案引用

| 方案决策 | 执行情况 |
|---------|---------|
| 不改源码，纯加测试 | ✅ 严格遵循 |
| Mock 隔离外部依赖 | ✅ sys.modules + unittest.mock.patch |
| 第 1 批覆盖 9 个零覆盖模块 | ⚠️ 完成 7/9（mcp_server, test_routes 搁置） |
| 第 2 批覆盖 6 个高 ROI 模块 | ⚠️ 完成 1/6（tool_registry/core） |

## 改动清单

| 文件 | 操作 | 目的 |
|------|------|------|
| `tests/unit/test_file_parser.py` | 新增 21 测试 | file_parser 0%→98% |
| `tests/unit/test_office_tools.py` | 新增 17 测试 | office_tools 0%→99% |
| `tests/unit/test_okf_importer.py` | 新增 18 测试 | okf_importer 0%→100% |
| `tests/unit/test_graph_query.py` | 新增 17 测试 | graph/query 0%→100% |
| `tests/unit/test_graph_watcher.py` | 新增 12 测试 | graph/watcher 0%→~90% |
| `tests/unit/test_ocr_tools.py` | 新增 20 测试 | ocr_tools 0%→100% |
| `tests/unit/test_gui_tools.py` | 新增 11 测试 | gui_tools 0%→100% |
| `tests/unit/test_tool_registry_core.py` | 新增 19 测试 | tool_registry/core 70%→增量 |

**总计**：8 新文件，135 新测试，全部通过。

## 偏差说明

1. **mcp_server.py (95行) 搁置**：JSON-RPC over stdio 协议，需完整 mock MCP 协议栈，投入产出比低
2. **testing/api/test_routes.py (73行) 搁置**：FastAPI 路由测试，依赖完整的 app context + orchestrator，Mock 链路长
3. **第 2 批仅完成 1/6**：高 ROI 模块（scheduler/runner, prompt/builder, hallucination/l5_z3）复杂度高，需单独迭代
4. **覆盖率增量低于预期**：零覆盖模块多为小文件（30-95 行），累计 ~500 语句 ≈ +2%。真正的大缺口在 `integration/wiring.py`（278 缺失）、`knowledge/mcp_server.py`（243 缺失）、`testing/orchestrator.py`（192 缺失）等 300-600 行的大文件

## 回溯对照

| PRD 验收标准 | 方案决策 | 代码位置 |
|-------------|---------|---------|
| 覆盖率 ≥80% | 3 批次策略 | ❌ 未达标（~72%），需继续迭代 |
| 零覆盖模块 → 0 个 | 第 1 批 9 模块 | ⚠️ 7/9 完成 |
| 核心模块不下降 | 只加测试 | ✅ 无源码变更 |
| 不引入新失败 | 每批验证 | ✅ 135/135 通过，全量回归 0 新增失败 |

## 下一迭代建议

### 最高 ROI 目标（按缺失语句排序）
| 模块 | 缺失语句 | 当前% | 预估增益 |
|------|---------|-------|---------|
| `integration/wiring.py` | 278 | 47% | +1.1% |
| `knowledge/mcp_server.py` | 243 | 58% | +1.0% |
| `testing/orchestrator.py` | 192 | 24% | +0.8% |
| `tools/video_tools.py` | 124 | 28% | +0.5% |
| `tools/registry/core.py` | 99→? | 70% | +0.3% |
| `scheduler/offpeak/scheduler.py` | 89 | 39% | +0.4% |
| `scheduler/merge_engine.py` | 70 | 57% | +0.3% |

### 当前剩余零覆盖
- `tools/mcp_server.py` (95行)
- `testing/api/test_routes.py` (73行)
- `testing/api/__init__.py` (1行)

### 策略
当前 ~72% → 80% 缺口 ~8%（~2,020 语句）。需要覆盖上述 TOP 5 大模块（合计 ~900 语句 ≈ +3.6%）+ 分支覆盖补强。建议第 2 次迭代聚焦 `integration/wiring.py` + `knowledge/mcp_server.py` + branch coverage。
