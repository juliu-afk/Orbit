# 阶段1 PRD：Orbit 覆盖率 69%→95% 冲刺

> 版本 1.0 | 2026-07-02 | 状态：待用户确认

---

## 1. 背景

Orbit 当前覆盖率 69%（13,392 statements，3,597 missed）。此前 sprint-2 达到 ≥80%，但新模块新增 + 部分测试退化导致回落到 69%。目标：95%。

现有测试基础设施：`tests/lib/` 已包含 mocks (7)、factories (10)、builders (4 chain)、scenarios (7)、generators、assertions。9 个覆盖率专项测试文件共 2,501 行。

## 2. 失败测试根因分析（26 失败/错误）

| # | 测试 | 数量 | 根因 | 修复 |
|---|------|------|------|------|
| 1 | `test_offpeak_scheduler.py` | 13 | Windows 缺 `tzdata` 包 → `ZoneInfoNotFoundError` | `uv add tzdata` |
| 2 | `test_memory.py` | 2 | Memory score 不递增（1.0→仍 1.0）——评分逻辑变更 | 更新断言 |
| 3 | `test_dream.py` | 1 | Dream status `complete`→`rejected`——引擎逻辑变更 | 更新断言 |
| 4 | `test_pr1_coverage.py` | 1 | `lifespan_handlers`→`lifespan_context`（lifespan 迁移副作用） | 改属性名 |
| 5 | `test_ws_router.py` | 1 | `NameError: name 'task' is not defined`——代码破损 | 修复变量引用 |
| 6 | `test_health_api.py` | 1 | Redis 不可用 → health 返回 `degraded` | Mock Redis |
| 7 | `test_task_runner_coverage.py` | 1 | GBK 编码——中文断言乱码 | 修复编码 |
| 8 | `test_react_agent_coverage.py` | 1 | Stream 事件数 1→2（lifespan/event 变更） | 更新断言 |
| 9 | `test_e2e_circuit_breaker.py` | 1 | E2E——health degraded（同 #6） | Mock Redis |
| 10 | `test_e2e_hallucination.py` | 1 | E2E——health degraded（同 #6） | Mock Redis |
| 11 | `test_phase4_e2e.py` | 1 | 集成测试——需排查 | 排查修复 |
| 12 | `test_perf_eventbus.py` | 2 | 性能测试 ERROR | 排查修复 |

## 3. 覆盖率缺口优先级

| 优先级 | 模块群 | Missed | 策略 |
|--------|--------|--------|------|
| P0 | 失败测试修复 | — | 修 26 个，回血 ~3-5% |
| P1 | 路由层 (chat/git_routes/codegraph_routes/blame_routes/search_routes/tests_routes) | 354 | TestClient + mock 依赖，投入低回报高 |
| P2 | Goal 子模块 (meta_orchestrator/subtask_session/intake_router/critique/verifier/ensemble) | 499 | 纯逻辑 mock，模块独立 |
| P3 | 纯函数模块 (probes/offpeak_scheduler/claude_md_generator) | 479 | 无外部依赖，最快覆盖 |
| P4 | 其余缺口 | ~2,265 | 按实际推进调整 |

## 4. 验收标准

- [ ] AC-1: 零失败测试——`pytest -q` exit code 0
- [ ] AC-2: 覆盖率 ≥95%——`pytest --cov --cov-fail-under=95`
- [ ] AC-3: 核心模块（调度器/防幻觉）纯函数 100%
- [ ] AC-4: 所有新测试可通过 `tests/lib/` 工厂/Mock 在一分钟内理解
- [ ] AC-5: 零新依赖引入（除 `tzdata` 系统修复）

## 5. Non-Goals

- 不新增功能模块测试覆盖以外的测试（不扩 scope）
- 不修改生产代码逻辑（仅修测试）
- 不碰前端测试覆盖率

## 6. 待确认

1. `tzdata` 包安装确认？（Windows 时区必需，纯系统依赖）
2. 本轮是否允许修改生产代码中明显的 bug？
3. 覆盖率策略：优先堆量（多模块浅覆盖）还是深挖（少模块 100%）？
