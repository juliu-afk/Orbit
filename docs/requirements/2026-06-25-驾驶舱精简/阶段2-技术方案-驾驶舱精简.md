# 技术方案 —— 驾驶舱界面精简

基于 [阶段1-PRD](阶段1-PRD-驾驶舱精简.md)，覆盖 4 条验收标准，无偏离。

## 需求回顾

| AC | 验收标准 |
|----|---------|
| AC1 | 聊天面板宽度 ≥ 65% 视口 |
| AC2 | 摘要面板 6 项信息（任务/Token/拦截/CB/告警/健康） |
| AC3 | 点击详情 → 抽屉展示 Token图 + 告警列表 + 健康面板 |
| AC4 | 仅改 1 个文件 |

## 布局结构

```
┌──────────────────────────────────────────────────┐
│ 🟢 已连接  📁 project  [session ▾]     [─] [✕] │  ← SessionBar (不变)
├────────────────────────────────┬─────────────────┤
│                                │  📊 活跃 3      │
│                                │  🔥 Token 12k   │
│   ChatPanel.vue               │  🛡️ 拦截 7     │
│   (flex: 1, min-width: 0)     │                  │
│                                │  ⚡ CB: ●●○     │
│                                │  ⚠️ 告警 0      │
│                                │  🔧 healthy     │
│                                │                  │
│                                │  [详情 ▸]       │
├────────────────────────────────┴─────────────────┤  ← input box spans both? No.
│  💬 输入框...                          [发送]    │    input box inside ChatPanel
└──────────────────────────────────────────────────┘
```

关键CSS: `.content-row { display: flex; height: calc(100vh - 120px); }`
- `.chat-col { flex: 0 0 70%; }` 聊天主区
- `.aside-col { flex: 0 0 30%; }` 右侧摘要

## 改动清单

仅改 `frontend/src/views/DashboardView.vue`：

1. **模板**：删除 metrics-row / cb-row / left-panel / bottom-row。替换为：
   - `.content-row` 包含 ChatPanel + 右侧摘要 `.aside-panel`
   - `.aside-panel` 内联 6 项摘要
   - `el-drawer` 承载详情（TokenChart + AlertList + HealthPanel）

2. **脚本**：新增 `showDetail` ref，保留所有已有 computed/store 引用不变

3. **导入**：移除未用导入（MetricsCard/CircuitBreakerLight/DagCanvas/TokenChart 移至 drawer 内使用，保留 import）

4. **样式**：删旧样式，加 `.aside-panel` + drawer 样式

## 验证

- `vue-tsc --noEmit` 通过
- `vite build` 通过
- 界面验证：聊天 70% + 摘要 30% + 抽屉展开
