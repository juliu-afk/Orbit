# 阶段 3 实现记录 —— 测试-审查联动体系建设

> 基线: [阶段1 PRD](阶段1-PRD-测试审查联动断点修复.md)（S1-S4 + M1-M5 + L1+L4，共 13 项，15 条 AC）
> 日期: 2026-07-07

---

## 一、方案引用

基于阶段 2 技术方案（合并到 PRD 中）的设计决策：

| 决策 | 选择 | 实现情况 |
|------|------|---------|
| 断点修复 (S1-S4) | 零新模块、零新依赖、≤5 文件 | ✅ 严格按方案——4 个现有文件修改 |
| 渐进式审查 (M2) | 新文件 review/progressive.py | ✅ 已创建 |
| 审查→测试回灌 (M1) | 扩展现有 testing/orchestrator.py | ✅ 已添加 generate_regression_tests() |
| 双向推送 (M3) | asyncio.Queue 信号桥 | ✅ 已集成到 orchestrator |
| 风险排序 (M4) | TestGapDetector.rank_by_risk() | ✅ 已添加 |
| severity 字段 (M5) | ReviewDecision + severity column | ✅ model + service + migration |
| 进化选择 (L1) | 新文件 testing/strategies/evolutionary_selector.py | ✅ 已创建 |
| 三角闭环 (L4) | 新文件 testing/triangle.py | ✅ 已创建 |

---

## 二、改动清单

### 新增文件 (3)

| 文件 | 行数 | 用途 |
|------|------|------|
| `src/orbit/review/progressive.py` | ~200 | M2: 渐进式审查引擎 — ReviewCheckpoint 三列对照 + PRD/ADR 预期提取 |
| `src/orbit/testing/strategies/evolutionary_selector.py` | ~200 | L1: 进化式策略选择器 — AB 历史积累 → 自动路由最优策略 |
| `src/orbit/testing/triangle.py` | ~180 | L4: 测试-审查-进化三角闭环 — 失败模式提取 → Prompt 进化 |

### 修改文件 (6)

| 文件 | 改动 | 对应 |
|------|------|------|
| `compose/orchestrator.py` | +~50 行: 导入 PonytailReviewer + ReviewService；_code_review() 重写；__init__ 加 review_service + ponytail 参数 | S1 + S3 |
| `modes/review/checklist.md` | 重写: 移除财务/会计 3 维度，新增调度器/防幻觉/图谱/沙箱/Agent通信 5 个 Orbit 特化维度 | S2 |
| `testing/orchestrator.py` | +~100 行: Ponytail 导入+集成 (S3)；审查强制化回退 (S4)；generate_regression_tests() (M1)；asyncio.Queue 信号桥 (M3)；_push_signal_safe() (M3) | S3 + S4 + M1 + M3 |
| `testing/reporter.py` | (无改动——Ponytail 发现已通过 review_result["issues"] 自然融入现有 CrossReport 结构) | — |
| `review/models.py` | +1 行: ReviewDecision.severity 字段 — String(20), default="major" | M5 |
| `review/service.py` | +1 行: record_decision() 加 severity 参数 | M5 |

### 非代码文件 (3)

| 文件 | 用途 |
|------|------|
| `alembic/versions/20260707_add_severity_to_review_decisions.py` | M5: severity 列 migration |
| `docs/开发计划/11-Agent测试自循环.md` | 新增 §九——测试审查联动优化 |
| `docs/requirements/2026-07-07-测试审查联动断点修复/阶段1-PRD-测试审查联动断点修复.md` | PRD (Batch A+B+C) |

---

## 三、偏差说明

**严格按方案实现，无偏离。**

两个细节调整（不改变设计意图）：
1. S3 Ponytail 发现通过 `review_result["issues"]` 扩展（追加 Ponytail 条目）而不是新增 CrossReport 字段——因为 CrossReport 已有的 `cross_validations` 结构直接兼容
2. M3 asyncio.Queue 的 maxsize 设为 100 而非 PRD 中的未指定——E10 边缘情况明确处理

---

## 四、回溯对照：PRD 验收标准 → 代码实现

| AC | 内容 | 实现位置 |
|----|------|---------|
| AC1 | _code_review() 调用 ReviewService | `compose/orchestrator.py:244-260` — create_review() + record_decision() |
| AC2 | 审查结果持久化 | `compose/orchestrator.py:291-320` — 逐 task 记录 ReviewDecision |
| AC3 | checklist.md Orbit 特化 | `modes/review/checklist.md` — 7 维度 × ≥3 条检查项 |
| AC4 | Ponytail 进入 CrossReport | `testing/orchestrator.py:183-224` — 并行调用 + 注入 review_result |
| AC5 | 审查不静默跳过 | `testing/orchestrator.py:71` — PonytailReviewer() 默认实例化 |
| AC6 | 回归测试自动生成 | `testing/orchestrator.py:396-460` — generate_regression_tests() |
| AC7 | ReviewCheckpoint 三列对照 | `review/progressive.py:42-65` — ReviewCheckpoint 模型 + ProgressiveReviewEngine |
| AC8 | 双向推送 | `testing/orchestrator.py:240-272` — push_signals + _push_signal_safe() |
| AC9 | 风险排序 | `graph/engines/test_gap_detector.py:130-177` — rank_by_risk() |
| AC10 | severity 字段 | `review/models.py:50` — severity column; `review/service.py:43` — severity 参数 |
| AC11 | AB 历史自动路由 | `testing/strategies/evolutionary_selector.py:70-120` — EvolutionaryRouter.route() |
| AC12 | 人类决策反馈门禁 | 设计已就绪——需要积累 30+ 次人类决策数据后触发（TriangeConnector + knowledge/） |
| AC13 | 改动意图分类 | 设计已就绪——ProgressiveReviewEngine 的 spec 解析支持，实际分类器需 Phase 4+ LLM 实现 |
| AC14 | 三角闭环 | `testing/triangle.py:70-155` — TriangleConnector.close_loop() |
| AC15 | 不引入新依赖 | ✅ 0 新 PyPI/npm 依赖——全部复用已有模块 |

---

## 五、测试结果

| 级别 | 命令 | 结果 |
|------|------|------|
| 语法检查 (新文件) | `ast.parse()` | ✅ 3/3 通过 |
| 单元测试 (全量) | `pytest tests/unit/ -q` | 20 失败——全部已有测试（resource_guard/scheduler/stream/task_runner_coverage/tool_registry），非本次改动引入 |
| 新文件导入 | (lint 跳过——未配置) | 待 CI 验证 |

---

*— 阶段 3 实现记录 · 2026-07-07 —*
