# 阶段3b-代码审查-多模态P1

> 基线：阶段1 PRD + 阶段2 技术方案 · 2026-07-10

## 审查清单

| # | 维度 | 结果 | 说明 |
|---|------|------|------|
| 1 | 安全：命令注入 | ✅ | yt-dlp/ffmpeg 参数来自内部逻辑，非用户输入 |
| 2 | 安全：硬编码密钥 | ✅ | `settings.ZAI_API_KEY` / `DEEPSEEK_API_KEY` |
| 3 | 方案偏差 | ✅ | 严格按方案：watch_video + ocr_document + video_capture + SKILL |
| 4 | 回溯一致性 | ✅ | SC1→video_tools, SC2→ocr_tools, SC3→video_capture, SC4→watch.md |
| 5 | 测试覆盖 | ✅ | 23 tests (12 FrameBudget+parse + 11 ScreenshotCapture) |
| 6 | 代码质量 | ✅ | 注释 WHY 中文，函数<200行 |

## 问题

**P2：yt-dlp/ffmpeg 未做可用性检测。** Tool 注册时不检查系统 PATH，首次调用时才发现缺失。
→ P1 改进：注册时 `shutil.which("yt-dlp")` 检测。

**P2：视频下载无进度回调。** 大文件下载用户无感知。
→ P1 改进：yt-dlp 加 `--progress` 回调。

## 审查结论：✅ 通过（0致命 0严重）
