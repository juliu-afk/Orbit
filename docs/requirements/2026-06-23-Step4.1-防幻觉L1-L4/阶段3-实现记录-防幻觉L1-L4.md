# 阶段3 实现记录 — Step 4.1 防幻觉 L1-L4

## 方案引用

基于阶段2技术方案（10 要素全覆盖），严格按方案实现，无偏离。

- 方案设计决策：4 验证器独立实现、L3 不集成流式（留 Step 5.1）、L1 跳过 builtins
- 均按方案执行

## 改动清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `src/orbit/hallucination/__init__.py` | 新增 | 包初始化，导出所有公共接口 |
| `src/orbit/hallucination/schemas.py` | 新增 | ValidationResult / L3EntropyConfig / 4 异常类 |
| `src/orbit/hallucination/l1_graph.py` | 新增 | AST 提取符号 → CodeGraphEngine.exists() 批量验证 |
| `src/orbit/hallucination/l2_dynamic.py` | 新增 | sys.settrace 包装注入 → Sandbox.run() 追踪 |
| `src/orbit/hallucination/l3_entropy.py` | 新增 | 滑动窗口归一化熵 + 重复度降级策略 |
| `src/orbit/hallucination/l4_type.py` | 新增 | mypy --strict subprocess + 错误解析 |
| `src/orbit/core/config.py` | 修改 | +12 行：ENABLE_L1-L4 + ENTROPY_THRESHOLD_* |
| `tests/unit/test_l1_graph.py` | 新增 | 6 用例（正向/不存在/空代码/语法错误/超时/builtins跳过） |
| `tests/unit/test_l2_dynamic.py` | 新增 | 6 用例（追踪/空代码/沙箱不可用/执行失败/无标记/代码注入） |
| `tests/unit/test_l3_entropy.py` | 新增 | 9 用例（低熵/高熵触发/窗口未满/降级/关闭降级/重置/边界/单选/空列表） |
| `tests/unit/test_l4_type.py` | 新增 | 6 用例（类型正确/类型错误/空代码/mypy未安装/超时/语法错误） |
| `CLAUDE.md` | 新增 | 项目级 CLAUDE.md（复制自 AGENTS.md） |

## 偏差说明

严格按方案实现，无偏离。

## 回溯对照

| AC | PRD 验收标准 | 方案设计决策 | 代码实现 |
|----|-----------|------------|---------|
| AC1 | 不存在符号 → GraphReferenceError | L1GraphValidator: AST + CodeGraphEngine.exists() | `src/orbit/hallucination/l1_graph.py:106-120` |
| AC2 | 高熵 ≥0.75 → HighEntropyError | L3EntropyMonitor: 滑动窗口归一化熵 | `src/orbit/hallucination/l3_entropy.py:65-80` |
| AC3 | 类型错误 → mypy 捕获 | L4TypeValidator: subprocess mypy --strict | `src/orbit/hallucination/l4_type.py:62-81` |
| AC4 | getattr 动态调用 → L2 追踪 | L2DynamicTracer: sys.settrace 注入 | `src/orbit/hallucination/l2_dynamic.py:78-115` |

## 测试结果

- 新增：27 用例
- 全量：126/126 通过
- 覆盖率：待阶段4a 正式报告
- 零回归
