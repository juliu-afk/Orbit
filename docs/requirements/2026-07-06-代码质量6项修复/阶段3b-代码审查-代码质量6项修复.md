# 阶段 3b 代码审查 —— 代码质量 6 项修复

## 审查范围

- 分支: `feat/code-quality-6items`
- 改动: 24 文件, +634/-732 行
- 类型: 非核心模块（不改调度器状态机/防幻觉层/核心模型）

## 逐项审查

### 安全
| 检查项 | 结果 | 说明 |
|------|:--:|------|
| SQL 注入 | ✅ | 无 SQL 改动 |
| XSS | ✅ | 无前端改动 |
| 命令注入 | ✅ | 无命令执行改动 |
| 硬编码密钥 | ✅ | 无密钥相关代码 |
| eval() | ✅ | 无 |

### 调度器
| 检查项 | 结果 | 说明 |
|------|:--:|------|
| 状态转换完整性 | ✅ | 未改状态机 |
| 检查点策略 | ✅ | checkpoint/manager 仅加 assert，不改逻辑 |
| 回滚路径 | ✅ | 未改 |

### 防幻觉
| 检查项 | 结果 | 说明 |
|------|:--:|------|
| L1-L9 链路影响 | ✅ | 未改防幻觉层 |
| 误报/漏报风险 | ✅ | 无 |

### 方案偏差
| 检查项 | 结果 | 说明 |
|------|:--:|------|
| AC1 except:pass 加日志 | ✅ | 6 处全部按方案执行，日志级别和 key 名正确 |
| AC2 clarifier 拆分 | ⚠️ | 实际拆为 4 文件（models/constants/agent/validators），方案原定 prompts/intent_parser/decision_tree/agent。偏差理由：代码实际结构以数据模型+校验函数为主，非 Prompt 模板+决策树。功能不变，拆分边界更准确 |
| AC3 CI pytest job | ✅ | 门禁 69，Python 3.11 |
| AC4 异常分层 | ✅ | gateway/client.py 加 _is_api_error()，checkpoint/manager 未改——降级链是设计意图，不改合理 |
| AC5 type:ignore 消除 | ⚠️ | 方案计划 16 处，实际消除 ~14 处。brief/storage.py 2 处保留（os.path.join 返回类型 vs Path.read_text 不匹配，属于 A 类合法保留）。wiring.py _monitor_queue 保留（asyncio.Task 无法加属性声明） |

### 回溯一致性
| PRD 验收标准 | 代码→方案→PRD | 状态 |
|------|:--:|:--:|
| AC1.1-1.7 | `react_agent.py:238→WARNING`, `react_agent.py:371→DEBUG`, `wiring.py:240/248→WARNING`, `task_runner.py:286/830/837→WARNING`, `schedule.py:125→WARNING` | ✅ |
| AC2.1-2.4 | `clarifier/{__init__,agent,models,constants,validators}.py`, import 路径不变, 12/12 测试通过 | ✅ |
| AC3.1-3.4 | `.github/workflows/ci.yml` test job, 3.11, pytest --cov-fail-under=69 | ✅ |
| AC4.1-4.3 | `gateway/client.py` _is_api_error() + asyncio.TimeoutError 区分 | ✅ |
| AC5.1-5.5 | chat/offpeak/observability/db_graph/checkpoint/projects/base/factory/orchestrator/main/feedback/task_runner | ✅ |

### 测试覆盖
| 检查项 | 结果 | 说明 |
|------|:--:|------|
| 核心模块正+异常用例 | ✅ | 现有 12 条 clarifier 测试全部通过 |
| 新代码是否对应测试 | N/A | 质量改进，未新增功能——现有测试验证向后兼容 |

### 代码质量
| 检查项 | 结果 | 说明 |
|------|:--:|------|
| 三行相似不抽象 | ✅ | 无新增重复代码 |
| 过早抽象 | ✅ | 无——_is_api_error() 提取是合理的 DRY |
| 边界条件 | ⚠️ | cast(DeferredStatus, row["status"]) 无运行时校验——但 DB 只存有效枚举值，风险极低 |
| 空值安全 | ✅ | assert 窄化 Optional 类型在核心路径，生产环境未用 python -O |

## 发现的问题

### P2-1: offpeak_scheduler cast 缺少运行时校验
- **位置**: `offpeak_scheduler.py:600`
- **描述**: `cast(DeferredStatus, row["status"])` 仅告诉 mypy 信任类型，不做运行时校验
- **风险评估**: 低——DB INSERT 时已约束为 DeferredStatus 枚举值，非法值不可能出现
- **建议**: 当前不改，若后续 DB 直接写入出现则改为 `assert row["status"] in get_args(DeferredStatus)`

### P2-2: brief/storage.py type:ignore 保留
- **位置**: `brief/storage.py:43,201`
- **描述**: `os.path.join()` 返回 `str` 类型，但调用方传 `Path` 对象时运行时返回 `Path`（Python 3.12+ 特性），类型系统无法表达
- **判定**: 合法保留，已在新方案中归入 A 类

### P2-3: wiring.py _monitor_queue type:ignore 保留
- **位置**: `wiring.py:269`
- **描述**: `task` 是 `asyncio.Task`（非 Orbit 模型），无法加属性声明
- **判定**: 合法保留。已在 A 类中说明理由

## 审查结论

**通过（无致命/严重问题）**

3 个 P2 发现均无需阻塞合并——offpeak cast 有 DB 约束保护，brief/wiring 的 ignore 是类型系统局限性。

> 审查方式：AI 自查（非核心模块改动，无需 `/ce-code-review` 12 Agent 并行审查）
