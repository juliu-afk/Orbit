# Phase 3b 代码审查 — 图谱工具升级 + Serena 卸载 + OKF 导出

> 审查日期：2026-07-10 | 审查人：AI 自审

## 改动范围

| 文件 | +/- | 操作 |
|------|-----|------|
| `src/orbit/knowledge/mcp_server.py` | +540 | 新增 9 工具 + `_run_async` 助手 + 旧 handler 重构 |
| `src/orbit/knowledge/okf_exporter.py` | **新文件** | OKF v0.1 导出器 |
| `configs/mcp_clients.yaml` | -14 | 删除 Serena 配置 |
| `src/orbit/tools/mcp_client.py` | -2 | 删除 docstring 示例 |
| `src/orbit/prompt/builder.py` | -1 | 删除注释引用 |
| `src/orbit/api/main.py` | -1 | 删除注释引用 |
| `tests/unit/test_mcp_client.py` | -3 | 泛化 `serena` → `test_server` |

## 审查清单

### 安全（致命）

| 项 | 结果 |
|----|------|
| SQL 注入 | ✅ PASS — 所有查询走 CodeGraphEngine SQLAlchemy 参数化查询 |
| XSS | ✅ PASS — MCP JSON-RPC stdio，无 Web 渲染 |
| 命令注入 | ✅ PASS — 无 `os.system`/`subprocess` shell=True |
| eval() | ✅ PASS — 无 eval/execec |
| 硬编码密钥 | ✅ PASS — 无密钥/Token |

### 方案偏差（严重）

| 项 | 结果 |
|----|------|
| 9 个新工具注册 | ✅ 按方案——5 查询 + 4 编辑 |
| OKF 导出器 | ✅ 按方案——`export()` + `generate_index()` + `generate_log()` |
| Serena 清理 | ✅ 按方案——6 文件清理，`rg -i serena` 仅剩注释性引用 |
| 零新依赖 | ✅ PASS — okf_exporter 手写 YAML 序列化避免 PyYAML |
| 不改 schema | ✅ PASS — CodeNode/Edge/KnowledgeStore 未动 |
| 不改前端 | ✅ PASS |
| **测试** | ⚠️ SERIOUS — 无新测试用例 |

### 回溯一致性（严重）

| PRD 验收标准 | 代码位置 | 状态 |
|-------------|---------|------|
| AC1 工具 4→13 | mcp_server.py:13 个 register_tool | ✅ |
| AC2 trace_path &lt;50ms | mcp_server.py:_handle_trace_path — BFS + visited set | ✅ |
| AC3 get_architecture &lt;2KB | mcp_server.py:_handle_get_architecture — 聚合查询 | ✅ |
| AC4 search_code 关联 CodeNode | mcp_server.py:_handle_search_code — 按行号匹配 | ✅ |
| AC5 编辑+增量索引 | mcp_server.py: replace/insert/delete handler 调 incremental_update | ✅ |
| AC6 Serena 零引用 | 6 文件清理完成 | ✅ |
| AC7 OKF 符合 v0.1 | okf_exporter.py: type 必需 + index.md + log.md | ✅ |

### 代码质量（一般）

| 项 | 发现 |
|----|------|
| `_run_async` 助手 | ✅ 消除 9 个 handler 中重复 ~10 行样板。ThreadPoolExecutor 每次创建——MCP 低频率场景可接受 |
| BFS visited 模式 | ✅ 已修复——visited check 在 add 前执行，防同层重复 |
| `_handle_replace_symbol_body` | ⚠️ 行号定位依赖 CodeNode.start_line/end_line 准确性。边界情况（嵌套函数、装饰器、lambda）可能偏移 1-2 行。不影响功能正确性——增量索引会修复 |
| `search_code` | ✅ 正则编译一次；200 文件截断防 OOM；100 匹配截断 |
| `okf_exporter.py` | ✅ 手写 YAML 序列化——零新依赖。中文 frontmatter 值处理正确 |
| `_handle_safe_delete_symbol` | ✅ 先查调用者再删——不会删仍有引用的符号 |

## 发现的问题

| # | 严重程度 | 描述 | 修复 |
|---|---------|------|------|
| 1 | 严重 | 无测试覆盖——9 新工具 + OKF 导出器均无自动化测试 | 未修复→Phase 4 补充 |
| 2 | 一般 | `_run_async` 每次创建 ThreadPoolExecutor | 已确认——MCP 低频可接受 |
| 3 | 一般 | `_handle_replace_symbol_body` 行号定位对嵌套函数可能偏移 | 已确认——增量索引会修复，不阻塞功能 |
| 4 | 致命→已修复 | okf_exporter.py 原用 PyYAML（不在依赖中） | ✅ 已改手写序列化 |
| 5 | 一般→已修复 | BFS visited 检查位置不正确 | ✅ 已修复 |

## 审查结论

**有条件通过** ⚠️

条件：Phase 4 补充至少以下测试：
- `test_trace_path_basic` — 已知调用链验证
- `test_get_architecture_returns_stats` — 架构概览字段完整性
- `test_search_code_matches_symbols` — 搜索结果关联 CodeNode
- `test_dead_code_no_callers` — 零调用者检测
- `test_okf_export_creates_bundle` — OKF 目录结构正确
- `test_safe_delete_refuses_with_callers` — 有调用者时拒绝删除
