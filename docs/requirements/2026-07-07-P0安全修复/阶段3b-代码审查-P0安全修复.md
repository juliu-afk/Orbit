# 阶段 3b 代码审查 — P0 安全漏洞修复

> 审查基线：阶段 2 技术方案 | 改动：4 文件，+23/-7 行

---

## 审查清单

### 安全（致命）

| 检查项 | 结果 | 说明 |
|--------|------|------|
| SQL 注入 | N/A | 无 SQL 改动 |
| XSS | N/A | 无 HTML 渲染改动 |
| 命令注入 | ✅ 通过 | P0-3 正是消除 `create_subprocess_shell` 注入面 |
| 硬编码密钥 | ✅ 通过 | 无新增密钥；token 读自 settings.ORBIT_AUTH_TOKEN |

### 方案偏差

| 文件 | 方案要求 | 实现 | 偏差 |
|------|---------|------|------|
| chat.py | accept 前 token 校验，gate on AUTH_ENABLED | ✅ `ws.query_params.get("token")` + `ws.close(4001)` | 无 |
| shell.py | create_subprocess_exec(*parts) | ✅ `shlex.split(cmd)` → `create_subprocess_exec(*parts, ...)` | 无 |
| chat.ts | WS URL 附加 `?token=` | ✅ `localStorage.getItem('orbitAuthToken')` → `encodeURIComponent` | 无 |
| test_shell.py | 更新 mock 路径 | ✅ 4 处 `_shell` → `_exec` | 无 |

**结论：严格按方案实现，无偏离。**

### 回溯一致性

| 验收标准 | 代码位置 | 验证 |
|---------|---------|------|
| AC-1: 无 token WS 拒绝 | [chat.py:99-104](src/orbit/api/routes/chat.py#L99-L104) | `if not token or token != settings.ORBIT_AUTH_TOKEN: ws.close(4001)` |
| AC-2: 有效 token WS 正常 | [chat.py:106](src/orbit/api/routes/chat.py#L106) | 通过校验后 `await ws.accept()` |
| AC-3: HTTP 认证不变 | dependencies.py | 零改动 |
| AC-4: exec 模式 | [shell.py:260-268](src/orbit/tools/shell.py#L260-L268) | `create_subprocess_exec(*parts, ...)` |
| AC-5: 白名单不变 | [shell.py:140-212](src/orbit/tools/shell.py#L140-L212) | `validate_command()` 零改动 |
| AC-6: 测试通过 | 42 passed | shell + chat 全绿 |

### 测试覆盖

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 新代码有测试 | ⚠️ 部分 | shell exec 切换已有 mock 覆盖；WS 认证拒绝场景缺少测试 |
| 核心模块正+异常 | ✅ | shell validate_command 覆盖危险/白名单/管道/警告等 20+ 用例 |
| 回归测试 | ✅ | 42 个已有测试全部通过 |

### 代码质量

| 检查项 | 结果 |
|--------|------|
| 三行相似不抽象 | N/A — 改动简单直白 |
| 过早抽象 | N/A |
| 边界条件 | ✅ AUTH_ENABLED gate、空 token、encodeURIComponent |
| 注释 WHY | ✅ 每个改动点有中文注释说明理由 |

---

## 发现

| ID | 严重度 | 位置 | 问题 | 修复 |
|----|--------|------|------|------|
| R1 | 一般 | tests/unit/test_chat_api.py | 缺少 WS auth 拒绝测试 | ✅ 已修复——补 3 个用例（无 token 拒绝、错 token 拒绝、正确 token 通过） |

## 审查结论

**通过** ✅ — 0 致命，0 严重，R1 已修复。

测试结果：chat 10/10 + shell 42/42 + security + tools + permission = 全绿。
