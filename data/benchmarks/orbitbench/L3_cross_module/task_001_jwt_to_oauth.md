# L3-001: Migrate auth from JWT to OAuth2
## Task: Replace JWT token validation with OAuth2 bearer token flow across auth.py + middleware.py + config.py
## Verify: All endpoints accept Bearer token, JWT-only endpoints return 401
