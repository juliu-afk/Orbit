# 阶段2 技术方案 —— 批次A

> 基线：[阶段1 PRD](阶段1-PRD-批次A.md) | 日期：2026-07-09

## 一、PRD对照

| AC | 方案 |
|----|------|
| D3 4条 | spectral.py 拉普拉斯分解 + Fiedler二分 + 谱半径 |
| D9 4条 | mechanism.py VCG分配 + GOLDEN_ROUTE降级 |
| D14 4条 | pac_bounds.py 泛化界 + SCOPE阈值自适应 |
| D24 4条 | slicer.py AST→PDG→切片 |
| **16条全覆** | |

## 二、D3 图谱频谱

**新文件**: `graph/spectral.py`

```python
class SpectralAnalyzer:
    def analyze(adj_matrix: sp.spmatrix) -> SpectralReport:
        # L = D - A, eigsh(L, k=5, which='SM')
        # Fiedler = eigenvectors[:,1], partition = sign(fiedler)
        # spectral_radius = eigsh(L_norm, k=1, which='LM')
    def modularity_score(adj, partition) -> float
```

**修改**: `graph/meta_graph.py` impact_analysis() 用谱半径替代全遍历

**依赖**: `scipy.sparse.linalg`（已在项目中）

## 三、D9 博弈论

**新文件**: `compose/mechanism.py`

```python
class VCGAllocator:
    def allocate(tasks: list[Task], agents: list[AgentBid]) -> dict[Task, Agent]
    # 每个Agent对子任务报价(cost, capability)
    # VCG: 分配使 ∑value最大化 → 诚实报价是占优策略
    def compute_payment(task, agent, all_bids) -> float
```

**修改**: `compose/orchestrator.py` run_spec() 可选 VCG 模式（`ORBIT_VCG=1`）

## 四、D14 PAC泛化

**新文件**: `evolution/pac_bounds.py`

```python
class PACBound:
    def compute(H_size: int, m_samples: int, delta=0.05) -> float:
        # ε = √((ln|H| + ln(1/δ)) / (2m))
    def is_reliable(H_size, m, min_epsilon=0.3) -> bool
```

**修改**: 
- `gepa.py` 每代输出 `pac_epsilon`
- `scope.py` UPGRADE_THRESHOLD 从 PAC 计算而非硬编码3

## 五、D24 程序切片

**新文件**: `graph/engines/slicer.py`

```python
class ProgramSlicer:
    def forward_slice(code: str, line: int, var: str) -> set[int]
    def backward_slice(code: str, line: int) -> set[int]
    # AST→CFG→DDG→PDG→图可达性查询
```

**修改**: `hallucination/l1_graph.py` 错误定位时用切片精确标定影响范围

## 六、改动总表

| 类型 | 文件 | 行数 |
|------|------|------|
| 新增 | `graph/spectral.py` | ~160 |
| 新增 | `compose/mechanism.py` | ~120 |
| 新增 | `evolution/pac_bounds.py` | ~90 |
| 新增 | `graph/engines/slicer.py` | ~140 |
| 修改 | `graph/meta_graph.py` | +15 |
| 修改 | `compose/orchestrator.py` | +20 |
| 修改 | `evolution/gepa.py` | +10 |
| 修改 | `evolution/scope.py` | +10 |
| 修改 | `hallucination/l1_graph.py` | +10 |
| **合计** | **9文件** | **~575** |

**0新依赖**——scipy已在项目中，其余纯Python。
