# 阶段 3b 代码审查 — P1 安全漏洞修复（8 项）

> 审查基线：阶段 2 技术方案 | 改动：16 文件

---

## 审查清单

### 安全（致命）

| 检查项 | 结果 |
|--------|------|
| SQL 注入 | N/A — 无 SQL 改动 |
| XSS | N/A — 无渲染改动 |
| 命令注入 | ✅ P1-4 Layer4 强制拒绝未沙箱化的 shell |
| 硬编码密钥 | ✅ JWT_SECRET 读环境变量，默认随机 |

### 方案偏差

| P1 | 方案 | 实现 | 偏差 |
|----|------|------|------|
| P1-1 | SSE Header+Query 双接受 | ✅ `Header(None)` + `Query(None)` fallback | 无 |
| P1-2 | 条件导入+方法拆分 | ✅ `_HAS_APPCONTAINER` + `_run_windows_appcontainer/subprocess` | 无 |
| P1-3 | JWT + static token | ✅ `_verify_token()` + `create_jwt_token()` + `verify_jwt_token()` | 无 |
| P1-4 | Layer4 强制检查 | ✅ `ProcessSandbox().is_available()` → False | 无 |
| P1-5 | asyncio.Lock | ✅ `allow()` async + `guard_request()` async | 无 |
| P1-6 | 统一 constants | ✅ `security/constants.py` + guard + permission 引用 | 无 |
| P1-7 | dispatch 白名单 | ✅ `ToolEntry.allowed_agents` + dispatch 检查 | 无 |
| P1-8 | bandit -lll | ✅ `-lll` + `continue-on-error: true` | 无 |

**结论：严格按方案实现，0 偏离。**

### 回溯一致性

| AC | 代码位置 | 验证 |
|----|---------|------|
| AC-1: SSE Header | `dependencies.py:81-92` | `Header(None, alias="X-Orbit-Token")` + `Query(None, alias="token")` |
| AC-2: AppContainer | `process_sandbox.py:18-23,75-89` | 条件导入 + 方法拆分 + TODO |
| AC-3: JWT | `dependencies.py:32-64` | `create_jwt_token` + `verify_jwt_token` + `_verify_token` |
| AC-4: Layer4 强制 | `permission.py:95-112` | `ProcessSandbox().is_available()` → False + audit log |
| AC-5: TokenBucket Lock | `token_bucket.py:33-43` | `async with self._lock` |
| AC-6: 统一 constants | `security/constants.py` + `guard.py:14-16` + `permission.py:17,208-211` | 单一定义 |
| AC-7: dispatch 白名单 | `registry/core.py:319-322` | `entry.allowed_agents` + 拒绝 |
| AC-8: bandit -lll | `security.yml:59-61` | `-lll` + `continue-on-error: true` |
| AC-9: 测试通过 | 108 passed | 0 regression |

### 代码质量

| 检查项 | 结果 |
|--------|------|
| 三行相似不抽象 | N/A |
| 过早抽象 | N/A |
| 边界条件 | ✅ Header 优先+Query 降级、JWT import 降级、allowed_agents=None 兼容 |
| 注释 WHY | ✅ P1-1/3/4/5/6/7 标记 |

### 新依赖审查

| 依赖 | 版本 | 用途 | 风险 |
|------|------|------|------|
| PyJWT | 2.13.0 | JWT 生成/验证 | pip-audit CI 已覆盖 |

---

## 发现

无致命/严重问题。108 passed, 0 failed。

## 审查结论

**通过** ✅
