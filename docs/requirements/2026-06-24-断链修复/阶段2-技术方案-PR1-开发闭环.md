# PR1 ??????????????1?

> 5 ? PR ??? 1 ???"?? PRD ? Scheduler ?? ? Agent ???? ? ????"???????

---

## ?? Case ??

### 1.1 ?????orchestrator._run_agent?

| # | ?? | ?? |
|---|------|------|
| C1 | _run_agent ? factory.create(role) | AgentFactory ? create() ????? Agent ?? |
| C2 | agent.execute(AgentInput) | ?? agent.run(agent_context)?????? |
| C3 | role ????? | create() ??? AgentRole??? get_agent ??? |
| C4 | llm ??? agent | agent.llm = self.llm?BaseAgent ?? llm ??? |
| C5 | ?? 300s | asyncio.wait_for ?? |

### 1.2 Scheduler ??????

| # | ?? | ?? |
|---|------|------|
| C6 | main.py ??? Scheduler | ?? llm_client + event_bus + agent_factory |
| C7 | chat._handle_confirm ? task ? | asyncio.create_task(scheduler.run_task(task_id, prd)) |
| C8 | run_task ??? | ?????? task_created????? |
| C9 | EventBus ?? create_app | app = create_app(EventBus())??????? |

### 1.3 ?? task:update ??

| # | ?? | ?? |
|---|------|------|
| C10 | DashboardView WS switch ?? task:update | ? taskStore.handleTaskUpdate(payload) |
| C11 | taskStore ?? state/progress/dagNodes | DAG ???? |

---

## ???????5 ???

### ??1?`src/orbit/agents/factory.py`???

? create() ??????
```python
@classmethod
def create(cls, role, llm=None, **kwargs):
    """create = get_agent ???orchestrator ? create???????"""
    return cls.get_agent(role, llm=llm, **kwargs)
```

### ??2?`src/orbit/scheduler/orchestrator.py`???

_run_agent ??????
1. `agent = self._agent_factory.create(role)` ? ?? llm?`self._agent_factory.create(role, llm=self.llm)`
2. `result = await asyncio.wait_for(agent.run(agent_context), timeout)` ? ?? execute?
```python
from orbit.agents.base import AgentInput
agent_input = AgentInput(
    task=context.get("prd", ""),
    context=agent_context.to_dict() if hasattr(agent_context, "to_dict") else {},
    role=role,
)
output_obj = await asyncio.wait_for(agent.execute(agent_input), timeout=timeout)
output = output_obj.result.get("design") or output_obj.result.get("code") or str(output_obj.result)
```

### ??3?`src/orbit/api/main.py`???

app ????? Scheduler + EventBus?
```python
from orbit.events.bus import EventBus
from orbit.agents.factory import AgentFactory
from orbit.scheduler.orchestrator import Scheduler

_event_bus = EventBus()
_scheduler = Scheduler(
    llm_client=LLMClient(),
    event_bus=_event_bus,
    agent_factory=AgentFactory,
)
app = create_app(_event_bus)
# ?? scheduler ? chat ???
```

### ??4?`src/orbit/api/routes/chat.py`???

_handle_confirm ?????? run_task?
```python
import asyncio
# ?????
asyncio.create_task(_scheduler.run_task(task_id, prd_text))
```

### ??5?`frontend/src/views/DashboardView.vue`???

WS switch ? task:update ???
```typescript
case 'task:update':
  taskStore.handleTaskUpdate(msg.payload)
  break
```

---

## ????????????

```
???? PRD ? chat._handle_confirm
  ? create_task (task_id)
  ? asyncio.create_task(scheduler.run_task(task_id, prd))
  ? IDLE?PARSING?PLANNING(architect)?CODING(developer)?VERIFYING(reviewer)?DONE
  ? ?????? _publish_task_update ? EventBus ? WS ??
  ? ?? DashboardView switch task:update ? taskStore ? DAG ????
```

---

## ????

| ? | ?? | ?? |
|----|------|------|
| AgentFactory.create ?? | 2 | C1/C3 ???+?? |
| orchestrator _run_agent ???mock factory? | 2 | C2/C5 execute+?? |
| chat confirm ?? run_task | 2 | C7/C8 |
| ?? task:update ?? | 2 | C10/C11 |

---

## ??

| ?? | ?? |
|------|------|
| agent_factory ?????? | _run_agent ? _agent_factory.create() ? classmethod??????? |
| execute() ?? AgentOutput ?? AgentResult | ?? output_obj.result dict ?? |
| run_task ?????? | ? try/except + logger.error |
