# V14.1 系统测试体系完整报告

> **发布日期**: 2026-06-25 | **状态**: 测试体系设计定稿
> **核心目标**: 建立覆盖 10 个维度的完整测试体系，确保系统生产环境稳定性。

---

## 测试体系总览

| 指标 | 目标 |
|------|------|
| 测试维度 | 10 个 |
| 测试场景 | 200+ |
| 测试工具 | 30+ |
| 单元测试覆盖率 | ≥80% |
| 核心路径 E2E 通过率 | 100% |
| 高危安全漏洞 | 0 个 |
| 混沌实验通过率 | ≥90% |

---

## 一、单元测试

| 测试对象 | 具体内容 | 工具 | 通过标准 |
|---------|------|------|---------|
| 调度器状态机 | 状态转换逻辑、非法转换拦截、重试计数 | pytest + asyncio | 分支覆盖率 ≥90% |
| Agent 基类 | think/act/observe 三阶段循环、上下文传递 | pytest-mock | 100% 通过 |
| L0 验证器 | 推理链 5 个维度检查、置信度校验 | pytest | 所有验证规则覆盖 |
| L1-L9 各层 | 各验证层的独立逻辑 | pytest + mypy + z3 | 各层独立通过率 100% |
| 检查点管理器 | save/load/cleanup 逻辑 | pytest + fakeredis | 边界条件覆盖 |
| 审计链写入 | 审计表写入、哈希链验证 | pytest + asyncpg | 写入准确率 100% |
| 图谱查询接口 | query_graph 各类型查询、缓存逻辑 | pytest + sqlite3 | 查询准确率 100% |
| 图谱解析引擎 | AST/Tree-sitter 解析准确率 | pytest | 解析准确率 ≥99% |
| LiteLLM 客户端 | 调用重试、熔断器状态机、降级逻辑 | pytest + httpx-mock | 错误场景全覆盖 |
| RouterAgent | 复杂度评分、路由决策逻辑 | pytest | 决策逻辑准确率 ≥95% |
| AgentModelResolver | 配置优先级（CC_SWITCH/环境变量/默认） | pytest | 优先级正确率 100% |
| MCP 工具调用 | 工具注册、权限校验、限流逻辑 | pytest | 边界条件覆盖 |
| Tokenizer 估算 | Token 计数准确率 | pytest + tiktoken | 误差 ≤5% |

---

## 二、集成测试

| 测试对象 | 具体内容 | 工具 | 通过标准 |
|---------|------|------|---------|
| L0-L9 完整验证链 | 9 层串行验证、失败阻断、重试 | pytest + docker-compose | 端到端验证 ≤3 分钟 |
| Agent 间通信（A2A） | 消息总线、超时、熔断传播 | pytest-asyncio | 通信成功率 ≥99.9% |
| MCP 工具调用链 | Agent → MCP Server → 工具 → 返回 | pytest + MCP SDK | 调用成功率 ≥99.5% |
| LiteLLM 网关集成 | 多模型调用、成本追踪、缓存命中 | pytest + litellm | 响应时间 ≤2s |
| 检查点+审计链 | 检查点保存时同步写入审计表 | pytest + asyncpg + redis | 数据一致性 100% |
| Git 集成 | Diff 应用、提交、PR 创建 | pytest + gitpython | 操作成功率 ≥99% |
| Webhook 回调 | 任务完成时触发、重试、超时 | pytest + httpx-mock | 回调成功率 ≥99% |
| 微信小程序 Skill | 任务提交、状态查询、交付物获取 | 真机测试 + 小程序 IDE | 全流程 ≤30 秒 |

---

## 三、端到端测试（E2E）

| 测试场景 | 具体内容 | 工具 | 通过标准 |
|---------|------|------|---------|
| 完整任务流程（成功） | NL→澄清→方案→代码→验证→交付 | pytest + httpx | 全流程 ≤15 分钟 |
| 完整任务流程（失败重试） | 代码验证失败→重试→最终成功 | pytest + httpx | 重试 ≤3 次 |
| 完整任务流程（最终失败） | 验证失败 3 次→标记 FAILED→转人工 | pytest + httpx | 正确转入工状态 |
| 多任务并发 | 10-50 个任务同时提交 | locust + pytest | 无 OOM，P95 延迟 < 基线×1.5 |
| 检查点恢复 | 进程崩溃后重启，从断点恢复 | pytest + docker | 恢复 ≤30 秒，无重复执行 |
| 熔断器触发与恢复 | 连续失败→OPEN→冷却→HALF_OPEN→CLOSED | pytest + time-mock | 状态转换正确 |
| CC_SWITCH 切换 | 环境变量切换模型后，实际调用正确模型 | pytest + docker | 切换生效 ≤5 秒 |
| Web 驾驶舱入口 | 提交任务→实时监控→获取交付物 | cypress + playwright | UI 响应 < 2 秒 |

---

## 四、性能测试

| 测试项 | 目标 | 工具 | 通过标准 |
|--------|------|------|---------|
| 调度器并发 | 最大并发任务数 | locust | 4核8G 稳定 ≥20 任务并发 |
| 图谱查询延迟 | query_graph 响应时间 | locust + sqlite3 | P95 < 50ms |
| LiteLLM 网关吞吐 | QPS 上限 | locust | 单实例 ≥1000 QPS |
| 检查点保存延迟 | save_checkpoint P95 | locust + redis | P95 < 20ms |
| 审计写入性能 | 异步写入不阻塞主流程 | locust + asyncpg | 主流程延迟增加 ≤10ms |
| L1-L9 验证耗时 | 各层单独耗时统计 | timeit | L0<100ms, L1-3<500ms, L4-6<2s, L7-9<5s |
| 沙箱启动时间 | Docker 容器创建到就绪 | timeit | P95 < 200ms |
| 缓存命中率 | DeepSeek 缓存命中率 | 监控统计 | ≥80% |
| 内存占用 | 系统运行内存峰值 | psutil + Prometheus | < 600MB（基线） |
| 端到端总耗时 | 简单/中等/复杂任务 | 监控统计 | 简单<30s, 中等<5min, 复杂<15min |

---

## 五、可靠性测试

| 测试项 | 具体内容 | 工具 | 通过标准 |
|--------|------|------|---------|
| LLM API 故障 | 超时/5xx/4xx | chaos-mesh + mock | 熔断器正确触发 |
| 数据库故障 | PostgreSQL 连接断开/慢查询 | chaos-mesh | 降级到缓存，不崩溃 |
| Redis 故障 | 不可用/超时 | chaos-mesh | 降级到内存，不崩溃 |
| 图谱文件损坏 | SQLite 文件损坏 | 模拟注入 | 自动修复或报错明确 |
| 磁盘满 | 无法写入日志/检查点 | chaos-mesh | 优雅降级，不丢失已有数据 |
| 网络分区 | Agent 间通信中断 | chaos-mesh | 超时后熔断，不死锁 |
| 长时间运行 | 7×24 小时持续运行 | 持续压测 | 无内存泄漏、无性能退化 |
| Token 预算耗尽 | ResourceGuard 三级熔断 | 模拟预算耗尽 | WARN→GRACE→HALT 正确触发 |

---

## 六、安全测试

| 测试项 | 具体内容 | 工具 | 通过标准 |
|--------|------|------|---------|
| Prompt 注入 | 恶意指令注入尝试 | 手动 + OWASP 规则 | 100% 拦截 |
| 系统指令隔离 | 用户输入是否干扰 System Prompt | 手动测试 | 指令边界清晰 |
| JWT Token 验证 | 伪造/过期/篡改 Token | jwt + 手动测试 | 全部拒绝 |
| 审计链防篡改 | 哈希链完整性验证 | 手动修改数据 | 检测到篡改 |
| API 密钥加密存储 | 密钥不得明文存储 | 手动检查 | Fernet 加密 |
| 沙箱逃逸 | 恶意代码尝试逃离 Docker | 安全测试集 | 100% 拦截 |
| SQL 注入 | 图谱查询中的注入尝试 | sqlmap + 手动 | 参数化查询，100% 防护 |
| 敏感数据泄露 | 日志/审计中不得出现密钥 | 手动检查 | 敏感信息脱敏 |
| 权限提升 | 普通用户试图调用 Admin API | 手动测试 | 403 拒绝 |
| TLS/mTLS | 通信加密 | 网络抓包 | 全部加密 |

---

## 七、AI/LLM 专项测试

| 测试项 | 具体内容 | 通过标准 |
|--------|------|---------|
| 输出格式合规（L1） | JSON Schema 校验 | 合规率 ≥99.5% |
| 命名幻觉检测（L2） | 是否引用不存在符号 | 拦截率 ≥95% |
| 熵监控有效性（L3） | 高熵是否触发熔断 | 触发延迟 < 200ms |
| Z3 形式化验证（L4） | 核心算法验证 | 验证周期 ≤5s |
| 功能聚类沙箱（L5） | I/O 行为聚类准确率 | 采纳准确率 ≥95% |
| 合约验证（L6） | 正向+反向验证 | 覆盖检测率 ≥95% |
| VibeGuard（L7） | 代码安全扫描 | 漏洞拦截率 ≥99% |
| 配置漂移检测（L8） | 配置变更识别 | 检测准确率 ≥95% |
| 合规验证（L9） | 法规标准符合性 | 合规拦截率 ≥98% |
| RouterAgent 决策准确率 | 模型推荐与实际需求匹配度 | 准确率 ≥85% |
| AgentModelResolver 配置检测 | 优先级解析正确性 | 100% |

---

## 八、用户体验测试

| 测试项 | 通过标准 |
|--------|---------|
| 微信小程序端到端 | 任务完成率 ≥90% |
| Web 驾驶舱可访问性 | WCAG 2.1 AA |
| 自然语言交互容错 | 3 轮内澄清成功 ≥80% |
| 交互轮次 | 简单≤2轮, 中等≤3轮, 复杂≤5轮 |
| 首屏加载时间 | < 2 秒 |
| 实时状态推送延迟 | < 5 秒 |
| 错误信息可读性 | 用户理解率 ≥90% |

---

## 九、混沌工程

| 实验 | 通过标准 |
|------|---------|
| LLM API 延迟注入（5-30s） | 超时控制生效，不崩溃 |
| LLM API 错误注入（429/500） | 熔断器触发，降级生效 |
| 数据库连接池耗尽 | 优雅排队，不崩溃 |
| Redis 内存满 | 降级到 PG，不丢失数据 |
| 容器内存限制降至 50% | OOM 时优雅退出 |
| K8s Pod Kill | 自动恢复，任务从检查点恢复 |
| 网络丢包 10-30% | 重试机制生效 |
| DNS 故障 | 降级到缓存，不崩溃 |
| Z3 超时 | 30 秒超时，标记 unknown |

---

## 十、部署与环境测试

| 测试项 | 通过标准 |
|--------|---------|
| Docker 镜像构建 | 构建时间 < 5 分钟 |
| docker-compose 启动 | 5 分钟全部 healthy |
| K8s 部署 | 所有 Pod Running |
| 灰度发布（1→5→20→100%） | 流量切换正确，自动回滚触发正确 |
| 环境隔离（Dev/Test/Staging/Prod） | 各环境配置隔离 |
| Prometheus 指标采集 | 指标完整性 ≥95% |
| Grafana 仪表盘 | 数据正确性 100% |
| 告警规则 | 触发延迟 < 30 秒 |

---

## 测试执行频率

| 测试类型 | 频率 | 触发条件 |
|---------|------|---------|
| 单元测试 | 每次 PR | CI 自动（< 5 分钟） |
| 集成测试 | 每次 PR | CI 自动（< 15 分钟） |
| E2E 测试 | 每日 | 定时触发（< 1 小时） |
| 性能测试 | 每周 | 手动/定时触发 |
| 安全扫描 | 每日 | CI 自动执行 |
| 混沌实验 | 每月 | 手动触发（预发布环境） |
| AI 质量评估 | 每次发布 | 手动触发 |

---

## 测试工具链

| 类别 | 工具 |
|------|------|
| 单元/集成 | pytest, pytest-asyncio, pytest-xdist, pytest-cov, pytest-mock |
| E2E | pytest, httpx, playwright, cypress |
| 性能 | locust, k6 |
| 混沌 | chaos-mesh, gremlin |
| 安全 | OWASP ZAP, sqlmap, bandit, safety |
| AI 专项 | deepdiff, json-schema-validator, mypy, z3-solver |
| 可观测性 | Prometheus, Grafana, ELK, Jaeger |
| 基础设施 | docker-compose, helm, kubectl, Istio |
| 前端 | vitest, cypress, playwright |

---

## 覆盖率目标

| 类型 | 目标 |
|------|------|
| 单元测试覆盖率 | ≥80% |
| 集成测试覆盖率 | ≥70% |
| E2E 场景覆盖率 | 100% 核心路径 |
| 高危安全漏洞 | 0 个 |
| 中危安全漏洞 | ≤3 个 |
| 混沌实验通过率 | ≥90% |
| AI 质量评分 | ≥85/100 |

---

*— V14.1 系统测试体系完整报告 · 2026年6月25日 —*
