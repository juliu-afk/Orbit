# 代码审查——Step 2.3 智能路由 + 测试补全

> PR: feat(router): Step 2.3 RouterAgent 核心 + LiteLLM 集成 + API + 测试
> 审查时间: 2026-06-26
> 改动范围: 10 新增文件 + 4 修改文件, +1156/-16 行

---

## 审查清单

### 安全

| 检查项 | 结果 | 说明 |
|--------|:--:|------|
| SQL 注入 | ✅ | 无直接 SQL 拼接。审计字段通过 ORM |
| XSS | ✅ | 前端无 `v-html`/`dangerouslySetInnerHTML`，API 响应为 JSON |
| 命令注入 | ✅ | 无 `eval()`/`exec()`/`subprocess` 调用 |
| 硬编码密钥 | ✅ | 无。模型密钥从 `settings.ZAI_API_KEY` 读取 |
| LLM prompt injection | ✅ | RouterAgent 不直接调用 LLM，仅输出 Tier 推荐 |

### 引擎/架构

| 检查项 | 结果 | 说明 |
|--------|:--:|------|
| RouterAgent 评分逻辑 | ✅ | 5 维度加权 + clamp [0,100] + 26 单元测试覆盖 |
| Resolver 优先级 | ✅ | 5 级链式解析，每级有明确 return，不会穿透 |
| CC_SWITCH 解析 | ✅ | `,force` 预处理为 `\|force` 避免逗号拆分冲突 + 13 chaos 测试 |
| 调度器集成 | ✅ | RouterAgent 在 PLANNING 阶段调用，失败→WARN→继续（不阻断） |
| 降级策略 | ✅ | LLMClient 主模型失败→fallback GLM-4.7 Flash |

### API

| 检查项 | 结果 | 说明 |
|--------|:--:|------|
| 输入校验 | ✅ | SwitchRequest.model 有 `min_length=1`；agent_name 校验已知 Agent 列表 |
| 错误响应格式 | ✅ | 404 用 HTTPException + detail 字符串 |
| API key 泄漏 | ✅ | 无。API 端点不返回密钥 |

### 方案偏差

| 技术方案 | 实现 | 偏差 |
|---------|------|:--:|
| RouterAgent 5 维度评分 | ✅ 实现 | 无 |
| AgentModelResolver 5 级优先级 | ✅ 实现 | 无 |
| CC_SWITCH 解析器 | ✅ 实现 | 预处理改用 `\|` 临时标记替代逗号拆分 |
| LLMClient 接入 Resolver | ✅ 实现 | 无 |
| 审计表 model_source | ✅ LLMResponse 新增字段 | 审计表 DDL 待后续 migration |
| API 端点 | ✅ GET/POST 实现 | 无 |
| 驾驶舱面板 | ✅ Element Plus 表格 | 基本实现，"历史"按钮预留 |

### 回溯一致性

| AC | PRD → 方案 → 代码 |
|----|------|
| AC1 | `RouterAgent.evaluate()` → `router/agent.py:83` |
| AC2 | `AgentModelResolver.resolve()` → `router/resolver.py:46` |
| AC3 | `parse_cc_switch()` → `router/cc_switch.py:55` |
| AC4 | `LLMClient.generate()` 接受 resolver → `gateway/client.py:52` |
| AC5 | 降级：主模型失败→GLM-4.7 Flash → `gateway/client.py:73` |
| AC6 | `LLMResponse.model_source` → `gateway/schemas.py:40` |
| AC7 | 已有 `test_compliance.py` 14 测试 ✅ |
| AC8 | `test_router.py` 26 测试 → `tests/unit/test_router.py` |
| AC9 | 已有 `security.yml` bandit+pip-audit ✅ |

### 测试覆盖

| 模块 | 测试数 | 类型 |
|------|:--:|------|
| RouterAgent | 10 | 单元 |
| AgentModelResolver | 8 | 单元 |
| CC_SWITCH parser | 8 | 单元 |
| Compliance (L9) | 14 | 单元（已有） |
| Chaos | 13 | 混沌 |
| **合计** | **53** | — |

### 代码质量

| 检查项 | 结果 |
|--------|:--:|
| 三行相似→不抽象 | ✅ 无过度相似 |
| 过早抽象 | ✅ RouterAgent/Resolver 职责清晰分离 |
| 空值/边界 | ✅ 0 文件数、负文件数、超大文件数均有 chaos 测试 |
| 类型注解 | ✅ 所有 public 函数有类型签名 |

---

## 审查结论

**通过。** 无致命/严重问题。

### 遗留项（不阻塞合并）

1. 审计表 DDL migration 待 PR 合并后单独执行
2. 驾驶舱"历史"按钮待 Phase 2 接入实际数据
3. `router/__init__.py` 中 `from orbit.router.agent import ComplexityScore` 未在 `__all__` 中使用，但保留供外部引用
