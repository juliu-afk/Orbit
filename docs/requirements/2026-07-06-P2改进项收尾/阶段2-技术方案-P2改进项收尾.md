# 阶段2-技术方案-P2改进项收尾.md

## 需求回顾
基于阶段1 PRD，修复 Audit Report P2 项（排除测试覆盖类）。

## 影响范围

| # | 项目 | 文件 | 改动类型 |
|---|------|------|---------|
| 1 | Terminal 白名单 | `terminal_routes.py` | ✅ 已存在——报告过时 |
| 2 | 审计哈希链 | `observability/audit.py` | +hashlib +prev_hash +record_hash 列 |
| 3 | TestGapDetector 接线 | `codegraph_routes.py` | +GET /test-gaps 端点 |
| 4 | _redis_client None | `api/main.py` | if guard |
| 5 | HallucinationPipeline None | `hallucination/pipeline.py` | ✅ 已有 graph=None guard |
| 6 | build_for_role 缓存 | `context/prebuilder.py` | _instances 类字典 |
| 7 | PeakPromptData 去重 | `PeakPromptDialog.vue` | import from peak.ts |
| 8 | git_routes → _workspace | `git_routes.py` | 改用共享模块 |
| 9 | matcher 注释 | `context/matcher.py` | SessionStore 依赖说明 |
| 10 | l6_contract 注释 | `hallucination/l6_contract.py` | 多服务合现说明 |

## 数据流
- **审计哈希链**: `add()` → SHA256(prev_hash + row_data) → 写入 prev_hash/record_hash
- **TestGapDetector**: `GET /codegraph/test-gaps?function=X` → `TestGapDetector.detect(code_graph, X)` → gap 列表
- **Prefbuilder 缓存**: `build_for_role()` → `_instances.get(role)` → 命中返回缓存实例

## 与 PRD 对照
全部 10 项验收标准已实现，无偏离。
