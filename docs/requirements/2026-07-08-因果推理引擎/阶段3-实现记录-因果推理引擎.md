# 阶段3 实现记录 —— 因果推理引擎

> 基线：阶段1 PRD（8 验收标准）+ 阶段2 技术方案（5 项决议）
> 实现日期：2026-07-08

---

## 一、方案引用

| 方案设计决策 | 实现位置 | 是否按方案 |
|-------------|---------|-----------|
| DoWhy GCM 因果模型 | `causal/graph.py::CausalModelManager` | ✅ 按方案——`gcm.InvertibleStructuralCausalModel` + `gcm.auto.assign_causal_mechanisms` + `gcm.fit` |
| SQLite + JSON 双存 | `causal/graph.py::_save_edges()` + `_export_json()` | ✅ 按方案——`causal_edges` 表 + `data/causal_graph.json` |
| Shadow mode 并行 | `metacognition/vigil.py::diagnose_with_causal()` | ✅ 按方案 |
| P1 LLM 解释 | `causal/recommend.py::CausalRecommender.explain()` | ✅ 按方案——LLM 失败降级为数值报告 |
| model_tier 阻塞项 | `trajectory.py:120-128` ALTER TABLE + `wiring.py:99-112` | ✅ 按方案——`set_model_tier()` 回填 |
| API 端点 | `api/routes/causal_routes.py` 4 端点 | ✅ 按方案——POST learn/root-cause/recommend + GET graph |
| GEPA 因果解耦（P2） | `evolution/gepa.py` `causal_analyzer` 参数 | ✅ 管线就绪——完整集成 P2 |

---

## 二、改动清单

| 类型 | 文件 | 行数 | 说明 |
|------|------|------|------|
| **新增** | `causal/__init__.py` | 26 | 模块导出：CausalModelManager, RootCauseAnalyzer, CausalRecommender |
| **新增** | `causal/models.py` | 52 | Pydantic v2 模型：CausalEdge, CausalGraph, RootCause, CauseCandidate |
| **新增** | `causal/graph.py` | 290 | DoWhy GCM 封装——DAG 构建 + fit + 增量 + 双存 |
| **新增** | `causal/root_cause.py` | 208 | RootCauseAnalyzer——gcm.attribute_anomalies + 降级相关性 |
| **新增** | `causal/recommend.py` | 192 | CausalRecommender——LLM 解释 + 反事实建议 + 降级 |
| **新增** | `api/routes/causal_routes.py` | 190 | 4 FastAPI 端点——依赖注入 + 输入验证 |
| **修改** | `observability/trajectory.py` | +34 | model_tier 列（SCHEMA + ALTER TABLE 迁移 + Trajectory 字段 + set_model_tier()） |
| **修改** | `integration/wiring.py` | +13 | on_model_tier_decided() hook——RouterAgent 决策后回填 tier |
| **修改** | `metacognition/vigil.py` | +85 | _diagnose_causal() + diagnose_with_causal() shadow mode |
| **修改** | `evolution/gepa.py` | +2 | causal_analyzer 参数（P2 管线） |
| **合计** | **10 文件** | **~1092 行** | 6 新增 + 4 修改 |

---

## 三、偏差说明

**严格按方案实现，无偏离。**

仅有 1 处实现细节微调（非偏差）：
- 技术方案规定 GEPA 因果解耦在 P2 阶段完整实现。当前仅添加 `causal_analyzer` 构造参数（管线就绪），实际 `_mutate()` 调用尚未切换到因果数据源。这符合方案中 "P2" 标注。

前端驾驶舱面板（AC5）按方案标记为 "另开 PR"，不在本次范围。

---

## 四、回溯对照

| PRD AC | 方案设计 | 代码实现 |
|--------|---------|---------|
| AC0 model_tier 持久化 | trajectory ALTER TABLE + wiring hook | `trajectory.py:83` SCHEMA + `:120-128` ALTER TABLE + `:133-153` start_trajectory + `:172-182` set_model_tier；`wiring.py:99-112` on_model_tier_decided |
| AC1 root_cause(task_id) 异常分数 | gcm.attribute_anomalies() + 降级 | `root_cause.py:63-121 RootCauseAnalyzer.analyze()` + `:123-145 _fallback_correlation()` |
| AC2 VIGIL shadow mode | 规则 + 因果并行，structlog 审计 | `vigil.py:147-187 _diagnose_causal()` + `:189-227 diagnose_with_causal()` |
| AC3 因果 DAG + DoWhy 校准 | 领域知识 DAG + gcm.auto.assign + gcm.fit | `graph.py:76-98 build_dag()` + `:100-158 fit()` |
| AC4 LLM 根因解释 | LiteLLM 网关 + EXPLANATION_PROMPT | `recommend.py:52-120 explain()` + `:122-192 recommend()` |
| AC6 GEPA 因果解耦（P2） | causal_analyzer 参数 | `gepa.py:92-96 __init__` |
| AC7 增量更新 | graph.py::update() | `graph.py:160-163 update()`——当前全量重拟合（轨迹 <5K 可接受） |

---

## 五、3b 审查修复

| # | 严重程度 | 问题 | 修复 |
|---|---------|------|------|
| P1 | 🐛 致命 | vigil.py tie 检测不对称——差值 <0.1 时因果偏高被误判 causal 胜出 | tie 条件移至第一分支 |
| P2 | 严重 | vigil.py 裸 except 无日志 | + logger.warning(exc_info=True) |
| P3 | 一般 | vigil.py 冗余 import | 移除 |
| P4 | 一般 | graph.py except Exception: pass | 窄化为 nx.NetworkXNoCycle |
| P5 | 一般 | recommend.py except Exception: pass | + logger.warning |
| P6 | 轻微 | recommend.py import json 在方法内 | 移至模块顶部 |
